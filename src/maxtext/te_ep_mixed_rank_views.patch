diff --git a/transformer_engine/jax/cpp_extensions/ep.py b/transformer_engine/jax/cpp_extensions/ep.py
index ca70ea14..5d03459a 100644
--- a/transformer_engine/jax/cpp_extensions/ep.py
+++ b/transformer_engine/jax/cpp_extensions/ep.py
@@ -135,22 +135,23 @@ def ep_handle_mem_size(cfg: EpLayerConfig) -> int:
 def _leading_axis_ok(spec):
     """Validate an EP input spec; return ``(ok, ep_axis, outer_axes)``.
 
-    Leading dim is ``ep`` or a tuple ending in ``ep`` (outer dp/fsdp axes
-    first); all other dims must be replicated.
+    Leading dim is ``ep`` or a tuple containing ``ep`` plus any operand-local
+    outer axes; all other dims must be replicated. Outer axes are inferred from
+    the operand instead of global dp/fsdp resources so EP can use a compound
+    rank view that differs from dense GEMMs in the same program.
     """
     gsr = global_mesh_resource()
     ep_axis = gsr.ep_resource
-    outer_axes = tuple(a for a in (gsr.dp_resource, gsr.fsdp_resource) if a is not None)
     if len(spec) < 2 or ep_axis is None:
-        return False, ep_axis, outer_axes
+        return False, ep_axis, ()
     if any(ax is not None for ax in spec[1:]):
-        return False, ep_axis, outer_axes
+        return False, ep_axis, ()
     leading = spec[0]
     elts = leading if isinstance(leading, tuple) else (leading,)
     if ep_axis not in elts:
-        return False, ep_axis, outer_axes
-    allowed = set(outer_axes) | {ep_axis}
-    return all(a in allowed for a in elts), ep_axis, outer_axes
+        return False, ep_axis, ()
+    outer_axes = tuple(a for a in elts if a is not None and a != ep_axis)
+    return True, ep_axis, outer_axes
 
 
 def _ep_outer_axis():
@@ -190,22 +191,21 @@ def _ep_output_spec(*trailing):
 
 
 def _ep_spec_ok(spec, trailing_count):
-    """Leading dim shards along ep (and outer dp/fsdp when set); trailing dims
-    are replicated. JAX may collapse size-1 mesh axes to ``None`` or drop them,
-    so the leading entry is normalized to a set of named axes before comparing.
+    """Validate an EP output spec with operand-local outer axes.
+
+    The leading dimension must contain EP and may contain arbitrary outer axes;
+    every trailing dimension must remain replicated.
     """
     gsr = global_mesh_resource()
     ep_axis = gsr.ep_resource
-    outer = _ep_outer_axis()
-    if len(spec) != 1 + trailing_count:
+    if ep_axis is None or len(spec) != 1 + trailing_count:
         return False
     if any(ax is not None for ax in spec[1:]):
         return False
     leading = spec[0]
     elts = leading if isinstance(leading, tuple) else (leading,)
     actual = frozenset(a for a in elts if a is not None)
-    expected = {ep_axis} if outer is None else {ep_axis, outer}
-    return actual <= expected
+    return ep_axis in actual
 
 
 # ── ep_prepare ──────────────────────────────────────────────────────────────
@@ -901,8 +901,15 @@ class EpCombineBwdPrimitive(BasePrimitive):
     ):
         del is_outer, result_infos
         arg_shardings = tuple(a.sharding for a in arg_infos)
-        # EP-output leading (trailing dims auto-pad to None).
-        out_sharding = NamedSharding(mesh, _ep_output_spec())
+        # The combine cotangent carries the source-token leading sharding.
+        # Preserve it instead of consulting the single global dp/fsdp role.
+        grad_spec = arg_infos[1].sharding.spec
+        if len(grad_spec) < 2 or grad_spec[0] is None:
+            raise NotImplementedError(
+                "EpCombineBwd: grad leading dimension must carry the EP input"
+                f" sharding; got spec={grad_spec}."
+            )
+        out_sharding = NamedSharding(mesh, PartitionSpec(grad_spec[0]))
 
         def sharded_impl(handle_mem, grad):
             return EpCombineBwdPrimitive.impl(
diff --git a/transformer_engine/jax/cpp_extensions/gemm.py b/transformer_engine/jax/cpp_extensions/gemm.py
index f56f03e4..428967b5 100644
--- a/transformer_engine/jax/cpp_extensions/gemm.py
+++ b/transformer_engine/jax/cpp_extensions/gemm.py
@@ -1173,12 +1173,15 @@ class GemmPrimitive(BasePrimitive):
 
             if reduce_spec is not None:
                 if not collective_op.is_reduce_scatter:
+                    reduce_axes = reduce_spec if isinstance(reduce_spec, tuple) else (reduce_spec,)
                     if is_all_reduce_in_float32():  # For unittest only
-                        output = jax.lax.psum(output.astype(jnp.float32), reduce_spec).astype(
-                            out_dtype
-                        )
+                        output = output.astype(jnp.float32)
+                        for axis in reduce_axes:
+                            output = jax.lax.psum(output, axis)
+                        output = output.astype(out_dtype)
                     else:
-                        output = jax.lax.psum(output, reduce_spec)
+                        for axis in reduce_axes:
+                            output = jax.lax.psum(output, axis)
 
                 if has_bias:
                     output += bias
diff --git a/transformer_engine/jax/ep.py b/transformer_engine/jax/ep.py
index 32074b49..88ee50a3 100644
--- a/transformer_engine/jax/ep.py
+++ b/transformer_engine/jax/ep.py
@@ -65,7 +65,8 @@ def _allgather_uid(uid_arr, world_size, uid_size):
     replicated = jax.sharding.NamedSharding(mesh, jax.sharding.PartitionSpec())
     local = np.asarray(uid_arr).reshape(1, uid_size)
     g_in = jax.make_array_from_process_local_data(sharded, local, (world_size, uid_size))
-    g_out = jax.jit(lambda x: x, out_shardings=replicated)(g_in)
+    with jax.set_mesh(mesh):
+        g_out = jax.jit(lambda x: x, out_shardings=replicated)(g_in)
     return np.asarray(g_out).reshape(world_size, uid_size)
 
 
@@ -100,6 +101,21 @@ def _ep_domain_for_rank(mesh, ep_resource, rank, device_to_rank=None):
     return int(grid[row, 0]), col, int(grid.shape[0])
 
 
+def _num_ep_output_groups(mesh_resource, axis_size_fn=get_mesh_axis_size):
+    """Count distinct EP output slabs from DP/FSDP resources, excluding TP."""
+    distinct_token_axes = tuple(
+        dict.fromkeys(
+            axis
+            for axis in (mesh_resource.dp_resource, mesh_resource.fsdp_resource)
+            if axis is not None
+        )
+    )
+    num_groups = 1
+    for axis in distinct_token_axes:
+        num_groups *= int(axis_size_fn(axis))
+    return num_groups
+
+
 def ep_bootstrap(
     world_size,
     rank,
@@ -168,15 +184,26 @@ def ep_bootstrap(
             f" world_size ({world_size})."
         )
     ep_size = get_mesh_axis_size(ep_resource)
-    # num_ep_groups counts only the distinct-token (dp/fsdp) axes; replicated
-    # axes (tp, pp, ...) do not create distinct EP-output slabs.
-    outer_axis = _ep_outer_axis()
-    num_ep_groups = 1 if outer_axis is None else get_mesh_axis_size(outer_axis)
+    if world_size % ep_size != 0:
+        raise ValueError(
+            f"ep_bootstrap: world_size ({world_size}) must be divisible by ep_size ({ep_size})."
+        )
+    # Communicators cover every fixed non-EP coordinate, including TP replicas.
+    expected_num_domains = world_size // ep_size
+    # EP outputs only distinguish token-owning DP/FSDP coordinates. TP is a
+    # replicated communicator dimension unless deliberately named as DP, as in
+    # MaxText's compound ETP1 view (dp=tensor, fsdp=fsdp).
+    num_ep_groups = _num_ep_output_groups(gsr)
     if num_experts % ep_size != 0:
         raise ValueError(f"num_experts ({num_experts}) must be divisible by ep_size ({ep_size}).")
 
     UID_SIZE = 128
-    root_rank, rank_within_group, _num_domains = _ep_domain_for_rank(mesh, ep_resource, rank)
+    root_rank, rank_within_group, num_domains = _ep_domain_for_rank(mesh, ep_resource, rank)
+    if num_domains != expected_num_domains:
+        raise ValueError(
+            f"ep_bootstrap: mesh has {num_domains} EP domains, expected"
+            f" world_size // ep_size = {expected_num_domains}."
+        )
     is_color_root = rank_within_group == 0
     if is_color_root:
         libnccl = ctypes.CDLL("libnccl.so.2", use_errno=True)
@@ -255,8 +282,15 @@ def _default_out_partition_spec():
 # ── ep_dispatch (custom_vjp) ─────────────────────────────────────────────────
 
 
-@partial(jax.custom_vjp, nondiff_argnums=(0, 4))
-def ep_dispatch(cfg, topk_idx, tokens, topk_weights, recv_capacity_per_rank):
+@partial(jax.custom_vjp, nondiff_argnums=(0, 4, 5))
+def ep_dispatch(
+    cfg,
+    topk_idx,
+    tokens,
+    topk_weights,
+    recv_capacity_per_rank,
+    out_sharding=None,
+):
     """Scatter tokens and weights to expert ranks.
 
     ``cfg`` is a per-layer ``EpLayerConfig``; distinct layers may share a
@@ -272,10 +306,24 @@ def ep_dispatch(cfg, topk_idx, tokens, topk_weights, recv_capacity_per_rank):
     the worst case, detect overflow by ``process_allgather``-ing it, then
     ``max(...) > recv_capacity_per_rank`` flags an overflowing step.
     """
-    return _dispatch_fwd(cfg, topk_idx, tokens, topk_weights, recv_capacity_per_rank)[0]
+    return _dispatch_fwd(
+        cfg,
+        topk_idx,
+        tokens,
+        topk_weights,
+        recv_capacity_per_rank,
+        out_sharding,
+    )[0]
 
 
-def _dispatch_fwd(cfg, topk_idx, tokens, topk_weights, recv_capacity_per_rank):
+def _dispatch_fwd(
+    cfg,
+    topk_idx,
+    tokens,
+    topk_weights,
+    recv_capacity_per_rank,
+    out_sharding=None,
+):
     if not jnp.issubdtype(topk_weights.dtype, jnp.floating):
         raise TypeError(
             f"ep_dispatch: topk_weights must be a floating dtype; got {topk_weights.dtype}."
@@ -289,11 +337,11 @@ def _dispatch_fwd(cfg, topk_idx, tokens, topk_weights, recv_capacity_per_rank):
     return primal, (handle_mem, out_leading)
 
 
-def _dispatch_bwd(cfg, recv_capacity_per_rank, res, g_outputs):
+def _dispatch_bwd(cfg, recv_capacity_per_rank, out_sharding, res, g_outputs):
     del recv_capacity_per_rank
     handle_mem, out_leading = res
     # Re-pin cotangent: XLA transpose can drop the EP axis and feed the FFI a global tensor.
-    out_spec = _default_out_partition_spec()
+    out_spec = _default_out_partition_spec() if out_sharding is None else out_sharding
     spec = jax.sharding.PartitionSpec(*out_spec)
     g_recv_tokens = with_sharding_constraint(g_outputs[0], spec)
     g_recv_topk_weights = with_sharding_constraint(g_outputs[1], spec)
